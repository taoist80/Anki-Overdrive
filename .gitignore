# Large binaries — sourced from archive.org, not version-controlled
*.xapk
*.apk
*.obb
*.zip

# Decompiled / rebuilt / extracted artifacts (derived from the APK — not committed)
*-decompiled/
*-jadx/
*-bundle/
obb-extract/
*-patched*.apk
*-aligned.apk
*-signed.apk
build/
dist/

# Signing material — NEVER commit
*.keystore
*.jks
keystore.properties

# OS / tooling
.DS_Store
.idea/
*.log

# Reverse-engineering artifacts (regenerable from Ghidra)
re/

# Extracted Unity assets (derived/proprietary)
assets-extracted/
obb-extract/

# Extracted game config (derived/proprietary) — backend reference only
reference/

# Bundled proprietary game assets in the app (derived from DDL/Anki — regenerate via extraction).
# Covers cars/, fonts/, gamedata/, and ui/ chrome art — all derived/proprietary.
android/app/src/main/assets/
